export const metadata = {
  title: "AI market insights · Block70",
  description:
    "AI-assisted crypto market analysis from Block70 signals, radar, and flows—with source lineage where stored. Research support only; not financial advice.",
};

export default function InsightsLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
