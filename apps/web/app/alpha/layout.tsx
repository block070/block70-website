export const metadata = {
  title: "Alpha research desk · Block70",
  description:
    "Institutional-style crypto research: community theses with discussion, model-ranked desk picks, and streaming alpha signals. High-signal context—not financial advice.",
};

export default function AlphaLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
