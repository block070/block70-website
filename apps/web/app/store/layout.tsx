import type { ReactNode } from "react";

export const metadata = {
  title: "Marketplace · Block70",
  description:
    "Plans, add-ons, and Blocks: subscriptions for signals and AI, API data access, and rewards.",
};

export default function StoreLayout({ children }: { children: ReactNode }) {
  return children;
}
